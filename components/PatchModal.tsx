'use client';
import { X } from 'lucide-react';import { FormEvent,useEffect,useState } from 'react';import { useIssues } from '@/context/IssueContext';import { useAuth } from '@/context/AuthContext';import type { PatchItem,PatchStyle } from '@/lib/types';
const styles:PatchStyle[]=['Business Formal','Psychopath / Chaos','Poetic / Chunnibyou','Casual','Corporate Passive-Aggressive'];
export function PatchModal({issueId,open,onClose,parentPatch}:{issueId:string;open:boolean;onClose:()=>void;parentPatch?:PatchItem|null}){const{addPatch}=useIssues();const{user,openAuth}=useAuth();const[text,setText]=useState('');const[style,setStyle]=useState<PatchStyle>('Casual');const[isSubmitting,setIsSubmitting]=useState(false);useEffect(()=>{if(!open){setText('');setStyle('Casual');setIsSubmitting(false);}},[open]);if(!open)return null;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!user){onClose();openAuth();return;}if(!text.trim()||isSubmitting)return;setIsSubmitting(true);try{await addPatch({issueId,text,style,parentPatchId:parentPatch?.id??null});onClose();}catch(error){console.error(error);setIsSubmitting(false);}}return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" onMouseDown={onClose}><div role="dialog" aria-modal="true" className="w-full max-w-lg rounded-3xl border border-white/10 bg-zinc-900 p-6" onMouseDown={e=>e.stopPropagation()}><div className="mb-6 flex items-start justify-between"><div><h2 className="text-xl font-bold">{parentPatch?'Re-Patch this Patch':'Submit a Patch'}</h2><p className="mt-1 text-sm text-zinc-500">{parentPatch?'Branch a new rewrite from this response.':'Make the message better, funnier, or stranger.'}</p></div><button onClick={onClose} aria-label="Close"><X size={18}/></button></div>{parentPatch&&<div className="mb-4 rounded-2xl border border-indigo-300/10 bg-indigo-300/[0.05] p-3 text-xs leading-5 text-zinc-400"><span className="font-semibold text-indigo-200">Parent Patch</span><div className="mt-1 line-clamp-3 text-zinc-500">{parentPatch.text}</div></div>}<form onSubmit={submit} className="space-y-4"><select value={style} onChange={e=>setStyle(e.target.value as PatchStyle)} className="w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm">{styles.map(s=><option key={s}>{s}</option>)}</select><textarea autoFocus value={text} onChange={e=>setText(e.target.value)} placeholder={parentPatch?'Write the next evolution…':'Write your rewrite…'} className="min-h-36 w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm" maxLength={800}/><button type="submit" disabled={!text.trim()||isSubmitting} className="w-full rounded-2xl bg-white px-4 py-3 text-sm font-bold text-zinc-950 disabled:opacity-40">{isSubmitting?'Publishing…':parentPatch?'Publish Re-Patch':'Publish Patch'}</button></form></div></div>}
