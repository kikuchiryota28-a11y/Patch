'use client';
import { useState } from 'react';
import { Dices, RefreshCw, Sparkles } from 'lucide-react';

const prompts=['You need to ask your friend to pay you back without making it awkward.','You accidentally left someone on read for three days.','You need to cancel plans at the last minute.','Your group chat message got absolutely no reaction.','You have to ask your teacher for an extension.','You want to invite someone somewhere without sounding desperate.','You sent a message to the wrong person.','You need to turn a painfully formal text into something human.'];
const styles=['Business Formal','Chaos','Poetic'];
export default function GachaPage(){const[prompt,setPrompt]=useState(prompts[0]);const[style,setStyle]=useState(styles[0]);function roll(){setPrompt(prompts[Math.floor(Math.random()*prompts.length)]);setStyle(styles[Math.floor(Math.random()*styles.length)]);}return <main className="min-h-screen"><div className="mx-auto max-w-3xl px-5 pb-24 pt-10 md:px-10 md:pt-16"><div className="mb-10"><div className="mb-3 inline-flex items-center gap-2 rounded-full border border-violet-300/15 bg-violet-300/[0.07] px-3 py-1.5 text-xs font-bold text-violet-200"><Dices size={13}/>Random Prompt Playground</div><h1 className="text-4xl font-black tracking-[-0.04em] md:text-6xl">Roll a problem.<br/><span className="text-zinc-500">Patch it your way.</span></h1><p className="mt-4 text-sm leading-7 text-zinc-500 md:text-base">A low-stakes playground for practicing the art of the rewrite.</p></div><section className="rounded-[2rem] border border-white/10 bg-white/[0.025] p-6 shadow-2xl shadow-black/20 md:p-10"><div className="mb-8 flex items-center justify-between"><span className="text-xs font-black uppercase tracking-[0.18em] text-zinc-600">Issue Gacha</span><span className="text-xs font-bold text-violet-200">{style}</span></div><div className="rounded-3xl border border-white/8 bg-zinc-950/80 p-7 md:p-10"><div className="mb-4 text-[11px] font-bold uppercase tracking-[0.18em] text-zinc-600">Your prompt</div><p className="text-xl font-bold leading-9 text-zinc-100 md:text-2xl">“{prompt}”</p></div><button onClick={roll} className="mt-6 flex w-full items-center justify-center gap-3 rounded-2xl bg-white px-5 py-4 text-sm font-black text-zinc-950 transition hover:bg-zinc-200 active:scale-[0.99]"><RefreshCw size={18}/>Roll again</button><div className="mt-7 flex items-center gap-2 text-xs text-zinc-600"><Sparkles size={14}/>No stakes. Just make the message better.</div></section></div></main>}
