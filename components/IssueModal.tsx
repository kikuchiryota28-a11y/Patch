'use client';
import { Bot, Dices, X } from 'lucide-react';
import { FormEvent, useEffect, useState } from 'react';
import { useIssues } from '@/context/IssueContext';
import { useAuth } from '@/context/AuthContext';
const GACHA_PROMPTS=[['Forgot to do my homework. What can I say to the teacher without making it worse?','I completely forgot the homework and class starts in 20 minutes.'],['How do I reject a terrible group chat invitation without making enemies?','I was invited to a group chat I absolutely do not want to join, but everyone is nice.'],['I am 45 minutes late to meet my friend. What do I text?','I overslept, they are already there, and I need an apology that actually works.'],['How do I leave a painfully awkward party early?','I need an exit message that sounds normal even though I have been here for ten minutes.']];
export function IssueModal({open,onClose}:{open:boolean;onClose:()=>void}){const{addIssue}=useIssues();const{user}=useAuth();const[title,setTitle]=useState('');const[body,setBody]=useState('');const[isSubmitting,setIsSubmitting]=useState(false);const[gachaPulse,setGachaPulse]=useState(false);useEffect(()=>{if(!open){setTitle('');setBody('');setIsSubmitting(false);setGachaPulse(false);}},[open]);if(!open)return null;function spinGacha(){const[t,b]=GACHA_PROMPTS[Math.floor(Math.random()*GACHA_PROMPTS.length)];setTitle(t);setBody(b);setGachaPulse(true);window.setTimeout(()=>setGachaPulse(false),500);}async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!user)return;if(!title.trim()||!body.trim()||isSubmitting)return;setIsSubmitting(true);try{const language=document.cookie.match(/(?:^|; )patch-locale=(en|ja)/)?.[1]==='ja'?'JA':'EN';await addIssue({title,body,category:'General',language});onClose();}catch(error){console.error(error);setIsSubmitting(false);}}return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" onMouseDown={onClose}><div role="dialog" aria-modal="true" className="w-full max-w-lg rounded-3xl border border-white/10 bg-zinc-900 p-6" onMouseDown={e=>e.stopPropagation()}><div className="mb-6 flex items-start justify-between"><div><h2 className="text-xl font-bold">Post a new Issue</h2><div className="mt-2 inline-flex items-center gap-1.5 rounded-full border border-indigo-300/20 bg-indigo-300/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.12em] text-indigo-200"><Bot size={11}/>3 AI Patches spawn automatically</div></div><button onClick={onClose} aria-label="Close"><X size={18}/></button></div><form onSubmit={submit} className="space-y-4"><div className="flex items-center justify-between gap-3"><span className="text-[11px] font-semibold uppercase tracking-[0.16em] text-zinc-600">Need an idea?</span><button type="button" onClick={spinGacha} className={`inline-flex items-center gap-2 rounded-full border border-indigo-300/15 bg-indigo-300/[0.08] px-3 py-2 text-xs font-bold text-indigo-200 ${gachaPulse?'scale-105':''}`}><Dices size={14}/>Issue Gacha</button></div><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Issue title" className="w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm" maxLength={120}/><textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Explain the situation" className="min-h-32 w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm" maxLength={600}/><button type="submit" disabled={!title.trim()||!body.trim()||isSubmitting} className="w-full rounded-2xl bg-white px-4 py-3 text-sm font-bold text-zinc-950 disabled:opacity-40">{isSubmitting?'Spawning patches…':'Publish Issue + Spawn Patches'}</button></form></div></div>}
