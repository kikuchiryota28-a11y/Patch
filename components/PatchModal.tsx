'use client';
import { X } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { useIssues } from '@/context/IssueContext';
import type { PatchStyle } from '@/lib/types';
const styles: PatchStyle[]=['Business Formal','Psychopath / Chaos','Poetic / Chunnibyou','Casual','Corporate Passive-Aggressive'];
export function PatchModal({issueId,open,onClose}:{issueId:string;open:boolean;onClose:()=>void}){const{addPatch}=useIssues();const[text,setText]=useState('');const[style,setStyle]=useState<PatchStyle>('Casual');if(!open)return null;function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!text.trim())return;addPatch({issueId,text,style});setText('');onClose()}return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" onMouseDown={onClose}><div role="dialog" aria-modal="true" className="w-full max-w-lg rounded-3xl border border-white/10 bg-zinc-900 p-6" onMouseDown={e=>e.stopPropagation()}><div className="mb-6 flex items-start justify-between"><div><h2 className="text-xl font-bold">Submit a Patch</h2><p className="mt-1 text-sm text-zinc-500">Make the message better, funnier, or stranger.</p></div><button onClick={onClose} aria-label="Close"><X size={18}/></button></div><form onSubmit={submit} className="space-y-4"><select value={style} onChange={e=>setStyle(e.target.value as PatchStyle)} className="w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm">{styles.map(s=><option key={s}>{s}</option>)}</select><textarea autoFocus value={text} onChange={e=>setText(e.target.value)} placeholder="Write your rewrite..." className="min-h-36 w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm" maxLength={800}/><button type="submit" disabled={!text.trim()} className="w-full rounded-2xl bg-white px-4 py-3 text-sm font-bold text-zinc-950 disabled:opacity-40">Publish Patch</button></form></div></div>}