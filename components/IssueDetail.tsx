'use client';
import Link from 'next/link';
import {ArrowLeft,Copy,MessageSquarePlus,Share2,Sparkles,Trophy,Flag} from 'lucide-react';
import {useEffect,useMemo,useState} from 'react';
import {Header} from '@/components/Header';
import {PatchModal} from '@/components/PatchModal';
import {PatchTree} from '@/components/PatchTree';
import {useIssues} from '@/context/IssueContext';
import {useAuth} from '@/context/AuthContext';
import {useLanguage} from '@/context/LanguageContext';
import {getActorId} from '@/lib/identity';
import {reportContent} from '@/lib/auth/actions';
import type {PatchItem} from '@/lib/types';
function clip(value:string,max:number){const clean=value.replace(/\s+/g,' ').trim();return clean.length>max?`${clean.slice(0,max-1)}…`:clean}
function buildShareText(before:string,after:string,url:string){return `Patch!\n\nBefore: ${clip(before,72)}\nAfter: ${clip(after,100)}\n\nPatch this yourself → ${url}`}
export function IssueDetail({id}:{id:string}){
 const{getIssue,upvotePatch,mergePatch}=useIssues();const{user,openAuth}=useAuth();const{dictionary}=useLanguage();const issue=getIssue(id);const issueStrings=dictionary.issue;
 const[open,setOpen]=useState(false);const[shareLabel,setShareLabel]=useState<string>(issueStrings.shareX);const[actorId,setActorId]=useState('');const[parentPatch,setParentPatch]=useState<PatchItem|null>(null);
 useEffect(()=>setActorId(getActorId()),[user]);useEffect(()=>setShareLabel(issueStrings.shareX),[issueStrings.shareX]);
 const bestPatch=useMemo(()=>issue?.patches.reduce((best,patch)=>(!best||patch.votes>best.votes?patch:best),issue.patches[0]),[issue]);
 if(!issue)return <main className="min-h-screen"><Header/><div className="mx-auto max-w-3xl px-4 py-20 text-center"><h1 className="text-2xl font-bold">{issueStrings.issueNotFound}</h1><Link href="/" className="mt-6 inline-block text-sm font-semibold text-indigo-300">{issueStrings.backHome}</Link></div></main>;
 const currentIssue=issue;
 function shareToX(){if(!bestPatch){setShareLabel(issueStrings.addPatchFirst);window.setTimeout(()=>setShareLabel(issueStrings.shareX),1800);return}const text=buildShareText(currentIssue.body,bestPatch.text,window.location.href);window.open(`https://x.com/intent/post?text=${encodeURIComponent(text)}`,'_blank','noopener,noreferrer,width=720,height=640');setShareLabel(issueStrings.openingX);window.setTimeout(()=>setShareLabel(issueStrings.shareX),1600)}
 async function copyShareText(){if(!bestPatch)return;await navigator.clipboard.writeText(buildShareText(currentIssue.body,bestPatch.text,window.location.href));setShareLabel(issueStrings.copied);window.setTimeout(()=>setShareLabel(issueStrings.shareX),1600)}
 function openPatch(parent:PatchItem|null){if(!user){openAuth();return}setParentPatch(parent);setOpen(true)}
 async function reportIssue(){if(!user){openAuth();return}const reason=window.prompt(issueStrings.reportReason,'other')?.trim().toLowerCase();if(!reason)return;try{await reportContent({targetType:'issue',targetId:currentIssue.id,reason});window.alert(issueStrings.reportRecorded)}catch(error){window.alert(error instanceof Error?error.message:issueStrings.reportFailed)}}
 return <main className="min-h-screen"><Header/><div className="mx-auto max-w-4xl px-4 pb-24 pt-8 md:px-6 md:pt-12"><Link href="/" className="mb-8 inline-flex items-center gap-2 text-xs font-semibold text-zinc-500 hover:text-zinc-200"><ArrowLeft size={14}/>{dictionary.home.homeBack}</Link><section className="rounded-3xl border border-white/8 bg-white/[0.025] p-6 shadow-glow md:p-8"><div className="mb-4 flex flex-wrap gap-2 text-xs text-zinc-500"><span className="rounded-full border border-white/8 px-2.5 py-1">@{currentIssue.author}</span><span>•</span><span>{currentIssue.language??'EN'}</span><span>•</span><span>{currentIssue.createdAt}</span><span>•</span><span className="rounded-full border border-white/8 px-2.5 py-1">{currentIssue.category}</span>{currentIssue.mergedPatchId&&<span className="inline-flex items-center gap-1 rounded-full border border-emerald-300/15 bg-emerald-300/[0.08] px-2.5 py-1 font-semibold text-emerald-200"><Trophy size={12}/>{issueStrings.officialMerged}</span>}</div><h1 className="max-w-3xl text-3xl font-black tracking-[-0.03em] md:text-5xl">{currentIssue.title}</h1><p className="mt-5 max-w-3xl whitespace-pre-line text-sm leading-7 text-zinc-400 md:text-base">{currentIssue.body}</p><div className="mt-7 flex flex-wrap items-center gap-3"><button type="button" onClick={()=>openPatch(null)} className="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2.5 text-sm font-bold text-zinc-950"><MessageSquarePlus size={16}/>{issueStrings.submitPatch}</button><button type="button" onClick={shareToX} className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-4 py-2.5 text-sm font-bold text-zinc-100"><Share2 size={16}/>{shareLabel}</button><button type="button" onClick={()=>void copyShareText()} disabled={!bestPatch} className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-3 py-2.5 text-xs font-semibold text-zinc-400 disabled:opacity-40"><Copy size={14}/>{issueStrings.copy}</button><button type="button" onClick={reportIssue} className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-3 py-2.5 text-xs font-semibold text-zinc-500 hover:text-zinc-200"><Flag size={13}/>{issueStrings.report}</button><div className="inline-flex items-center gap-2 rounded-full border border-white/8 px-4 py-2.5 text-xs font-semibold text-zinc-400"><Sparkles size={14}/>{currentIssue.patches.length} {currentIssue.patches.length===1?dictionary.home.patch:dictionary.home.patches}</div></div></section>{currentIssue.patches.length?<PatchTree patches={currentIssue.patches} onReply={openPatch} onUpvote={patch=>upvotePatch(currentIssue.id,patch.id)} canMerge={Boolean(actorId)&&currentIssue.ownerActorId===actorId} onMerge={patch=>mergePatch(currentIssue.id,patch.id)}/>:<div className="mt-10 rounded-3xl border border-dashed border-white/10 p-10 text-center text-sm text-zinc-500">{dictionary.home.emptyHint}</div>}</div><PatchModal issueId={currentIssue.id} open={open} parentPatch={parentPatch} onClose={()=>{setOpen(false);setParentPatch(null)}}/></main>
}
