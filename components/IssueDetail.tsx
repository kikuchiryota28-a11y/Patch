'use client';
import Link from 'next/link';
import { ArrowLeft, MessageSquarePlus, Sparkles } from 'lucide-react';
import { useState } from 'react';
import { Header } from '@/components/Header';
import { PatchCard } from '@/components/PatchCard';
import { PatchModal } from '@/components/PatchModal';
import { useIssues } from '@/context/IssueContext';
export function IssueDetail({id}:{id:string}){const{getIssue}=useIssues();const issue=getIssue(id);const[open,setOpen]=useState(false);if(!issue)return <main className="min-h-screen"><Header/><div className="mx-auto max-w-3xl px-4 py-20 text-center"><h1 className="text-2xl font-bold">Issue not found</h1><Link href="/" className="mt-6 inline-block text-sm font-semibold text-indigo-300">Back home →</Link></div></main>;return <main className="min-h-screen"><Header/><div className="mx-auto max-w-4xl px-4 pb-24 pt-8 md:px-6 md:pt-12"><Link href="/" className="mb-8 inline-flex items-center gap-2 text-xs font-semibold text-zinc-500 hover:text-zinc-200"><ArrowLeft size={14}/>Back to timeline</Link><section className="rounded-3xl border border-white/8 bg-white/[0.025] p-6 shadow-glow md:p-8"><div className="mb-4 flex gap-2 text-xs text-zinc-500"><span className="rounded-full border border-white/8 px-2.5 py-1">@{issue.author}</span><span>•</span><span>{issue.createdAt}</span></div><h1 className="max-w-3xl text-3xl font-black tracking-[-0.03em] md:text-5xl">{issue.title}</h1><p className="mt-5 max-w-3xl text-sm leading-7 text-zinc-400 md:text-base">{issue.body}</p><div className="mt-7 flex flex-wrap items-center gap-3"><button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-full bg-white px-4 py-2.5 text-sm font-bold text-zinc-950"><MessageSquarePlus size={16}/>Submit a Patch</button><div className="inline-flex items-center gap-2 rounded-full border border-white/8 px-4 py-2.5 text-xs font-semibold text-zinc-400"><Sparkles size={14}/>{issue.patches.length} Patches</div></div></section><section className="mt-10"><div className="mb-5"><div className="text-xs font-semibold uppercase tracking-[0.18em] text-indigo-300/80">The Patch stack</div><h2 className="mt-1 text-xl font-bold">How the internet would say it</h2></div><div className="space-y-8">{issue.patches.length?issue.patches.map(p=><PatchCard key={p.id} patch={p} before={issue.body}/>):<div className="rounded-3xl border border-dashed border-white/10 p-10 text-center text-sm text-zinc-500">No patches yet. Be the first.</div>}</div></section></div><PatchModal issueId={issue.id} open={open} onClose={()=>setOpen(false)}/></main>}