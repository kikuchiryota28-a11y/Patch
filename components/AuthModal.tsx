'use client';
import {useEffect,useState} from 'react';
import {Mail,X} from 'lucide-react';
import {getSupabaseClient} from '@/lib/supabase/client';
import {useAuth} from '@/context/AuthContext';
import {useI18n} from '@/components/I18nProvider';
import type {Locale} from '@/lib/i18n';

export function AuthModal(){
 const{user,authOpen,closeAuth,needsOnboarding,saveProfile}=useAuth();
 const{t,locale}=useI18n();
 const supabase=getSupabaseClient();
 const[email,setEmail]=useState('');const[password,setPassword]=useState('');
 const[username,setUsername]=useState('');const[displayName,setDisplayName]=useState('');const[bio,setBio]=useState('');const[avatarUrl,setAvatarUrl]=useState('');
 const[language,setLanguage]=useState<Locale>('en');
 const[mode,setMode]=useState<'signin'|'signup'>('signin');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);
 useEffect(()=>{if(user){setDisplayName(String(user.user_metadata?.full_name??user.user_metadata?.name??''));setAvatarUrl(String(user.user_metadata?.avatar_url??user.user_metadata?.picture??''))}},[user]);
 useEffect(()=>{if(needsOnboarding)setLanguage(locale)},[needsOnboarding,locale]);
 if(!authOpen&&!needsOnboarding)return null;
 async function submit(){if(!supabase||!email.trim()||password.length<6)return;setBusy(true);setMessage('');const result=mode==='signin'?await supabase.auth.signInWithPassword({email:email.trim(),password}):await supabase.auth.signUp({email:email.trim(),password});if(result.error)setMessage(result.error.message);else if(mode==='signup'&&!result.data.session)setMessage('Account created. Check your email to confirm it.');else closeAuth();setBusy(false)}
 async function magic(){if(!supabase||!email.trim())return;setBusy(true);setMessage('');const{error}=await supabase.auth.signInWithOtp({email:email.trim(),options:{emailRedirectTo:`${window.location.origin}/auth/callback?next=/`}});setMessage(error?.message??t('auth.magic'));setBusy(false)}
 async function finish(){setBusy(true);setMessage('');try{await saveProfile({username,displayName,bio,avatarUrl,language});closeAuth()}catch(e){setMessage(e instanceof Error?e.message:'Could not save profile.')}finally{setBusy(false)}}
 const onboarding=Boolean(user&&needsOnboarding);
 return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/85 p-4 backdrop-blur-xl" onMouseDown={()=>!onboarding&&closeAuth()}><div role="dialog" aria-modal="true" className="max-h-[90vh] w-full max-w-md overflow-y-auto rounded-[30px] border border-white/10 bg-zinc-900/95 p-6 shadow-[0_30px_100px_rgba(0,0,0,.6)] ring-1 ring-white/[0.03]" onMouseDown={e=>e.stopPropagation()}><div className="mb-6 flex items-start justify-between"><div><p className="text-[10px] font-black uppercase tracking-[0.24em] text-zinc-600">Patch!</p><h2 className="mt-2 text-2xl font-black tracking-[-0.03em]">{onboarding?'Finish your profile':t('auth.title')}</h2></div>{!onboarding&&<button onClick={closeAuth} aria-label={t('auth.close')} className="grid h-10 w-10 place-items-center rounded-full text-zinc-500 transition hover:bg-white/[0.06] hover:text-white"><X size={18}/></button>}</div>{onboarding?<div className="space-y-4"><Field label={t('settings.username')} value={username} setValue={setUsername} placeholder="patchsmith"/><Field label={t('settings.displayName')} value={displayName} setValue={setDisplayName} placeholder="Your name"/><Field label={t('settings.bio')} value={bio} setValue={setBio} placeholder="Tell people about you"/><Field label={t('settings.avatar')} value={avatarUrl} setValue={setAvatarUrl} placeholder="https://…"/><label className="block text-xs font-bold text-zinc-500">Language<select value={language} onChange={e=>setLanguage(e.target.value as Locale)} className="mt-2 w-full appearance-none rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3 text-sm text-white outline-none transition focus:border-white/25"><option value="en">English</option><option value="ja">日本語</option><option value="es">Español</option><option value="zh">中文</option></select></label><button onClick={finish} disabled={busy} className="w-full rounded-2xl bg-white px-4 py-3.5 text-sm font-black text-zinc-950 transition hover:-translate-y-0.5 hover:bg-zinc-100 disabled:translate-y-0 disabled:opacity-40">{busy?'Saving…':t('settings.save')}</button></div>:<div className="space-y-3"><input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder={t('auth.email')} className="w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3.5 text-sm text-white outline-none transition focus:border-white/25"/><input type="password" autoComplete={mode==='signin'?'current-password':'new-password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder={t('auth.password')} minLength={6} className="w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3.5 text-sm text-white outline-none transition focus:border-white/25"/><button onClick={submit} disabled={busy||!email.trim()||password.length<6} className="w-full rounded-2xl bg-white px-4 py-3.5 text-sm font-black text-zinc-950 transition hover:-translate-y-0.5 hover:bg-zinc-100 disabled:translate-y-0 disabled:opacity-40">{busy?'…':mode==='signin'?t('auth.signIn'):t('auth.signUp')}</button><div className="flex items-center gap-3 py-2 text-[10px] font-bold uppercase tracking-widest text-zinc-700"><span className="h-px flex-1 bg-white/5"/>{t('auth.or')}<span className="h-px flex-1 bg-white/5"/></div><div className="flex gap-2"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder={t('auth.email')} className="min-w-0 flex-1 rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3.5 text-sm text-white outline-none transition focus:border-white/25"/><button onClick={magic} disabled={busy||!email.trim()} className="inline-flex items-center gap-2 rounded-2xl bg-white px-4 py-3.5 text-sm font-black text-zinc-950 disabled:opacity-40"><Mail size={15}/>{t('auth.magic')}</button></div><button onClick={()=>{setMode(mode==='signin'?'signup':'signin');setMessage('')}} className="w-full py-2 text-xs font-semibold text-zinc-500 transition hover:text-white">{mode==='signin'?t('auth.switchSignup'):'Already have an account?'}</button></div>}{message&&<p className="mt-4 rounded-2xl border border-amber-300/10 bg-amber-300/[0.05] p-3 text-xs leading-5 text-amber-200">{message}</p>}</div></div>}
function Field({label,value,setValue,placeholder}:{label:string;value:string;setValue:(v:string)=>void;placeholder:string}){return <label className="block text-xs font-semibold text-zinc-400">{label}<input value={value} onChange={e=>setValue(e.target.value)} placeholder={placeholder} className="mt-1.5 w-full rounded-2xl border border-white/10 bg-zinc-950 px-4 py-3.5 text-sm text-white outline-none transition focus:border-white/25"/></label>}
